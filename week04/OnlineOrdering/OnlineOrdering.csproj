<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- Build this project as a runnable console application. -->
    <OutputType>Exe</OutputType>
    <!-- Use the same .NET version as the other projects in this course repository. -->
    <TargetFramework>net10.0</TargetFramework>
    <!-- Keep namespace imports and nullable annotations explicit in the source files. -->
    <ImplicitUsings>disable</ImplicitUsings>
    <Nullable>disable</Nullable>
  </PropertyGroup>

</Project>
